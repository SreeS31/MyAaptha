spring:
  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/${POSTGRES_DB:circlenet_ai}
    username: ${POSTGRES_USER:circlenet}
    password: ${POSTGRES_PASSWORD:${postgres_password}}
    driver-class-name: org.postgresql.Driver
  sql:
    init:
      mode: never
  flyway:
    enabled: true
    locations: classpath:db/migration/common
  jpa:
    hibernate:
      ddl-auto: none
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  h2:
    console:
      enabled: false

security:
  jwt:
    secret: ${JWT_SECRET:${jwt_secret}}

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      probes:
        enabled: true
      show-details: never
  metrics:
    tags:
      application: circlenet-backend

circlenet:
  rate-limit:
    # Enable only when the load balancer strips client-supplied forwarding headers.
    trust-forwarded-for: ${CIRCLENET_TRUST_FORWARDED_FOR:false}
    auth-per-minute: ${CIRCLENET_AUTH_RATE_LIMIT:10}
    api-per-minute: ${CIRCLENET_API_RATE_LIMIT:300}
  cors:
    allowed-origins: ${CIRCLENET_CORS_ALLOWED_ORIGINS}
  ai:
    base-url: ${CIRCLENET_AI_BASE_URL:http://ai-agent:8081/api/v1}
  notifications:
    webhook:
      channel: ${CIRCLENET_NOTIFICATION_WEBHOOK_CHANNEL:}
      url: ${CIRCLENET_NOTIFICATION_WEBHOOK_URL:https://sms-provider.invalid/api/messages}
      token: ${CIRCLENET_NOTIFICATION_WEBHOOK_TOKEN:}
